# ProxmoxClient

```
# Only the first time:
npm install

# Start project:
npm start

# Build Docker image:
docker build . -t proxmox-client

# Run Docker image:
docker run --name proxmox-client -p 8080:8080 proxmox-client
```
